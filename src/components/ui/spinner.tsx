import React from "react";

const Spinner = () => {
  return (
    <div className="h-5 w-5 animate-spin rounded-full border-2 border-solid border-current border-t-transparent text-gray-600"></div>
  );
};

export default Spinner;
